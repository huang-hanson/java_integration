# 应用服务 WEB 访问端口
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mappers/*xml
  type-aliases-package: com.hanson.mvc.domain

logging:
  level:
    com:
      hanson:
        mvc:
          mapper: DEBUG